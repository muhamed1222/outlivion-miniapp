# 🔐 Настройка Telegram Login Widget

> **Дата:** Декабрь 2025  
> **Статус:** ✅ Реализовано

---

## ✅ Что сделано

### 1. Создан компонент TelegramLoginButton
**Файл:** `src/components/TelegramLoginButton.tsx`

- ✅ Динамически загружает Telegram Widget script
- ✅ Настраивается через props (размер, радиус, язык)
- ✅ Автоматически вызывает callback при авторизации
- ✅ Поддерживает cleanup при unmount

### 2. Обновлена страница /web/login
**Файл:** `src/app/web/login/page.tsx`

- ✅ Заменена mock-кнопка на настоящий Telegram Login Widget
- ✅ Интегрирована с `loginWithTelegramWidget` из auth.ts
- ✅ Подключена к API endpoint `/auth/telegram`
- ✅ Обработка loading и error состояний
- ✅ Автоматический редирект после успешного входа

---

## 🤖 Настройка Telegram бота

### Шаг 1: Настроить домен в BotFather

```bash
# Откройте @BotFather в Telegram
/setdomain

# Выберите вашего бота (@outlivionbot)

# Введите домен (БЕЗ https://)
app.outlivion.space
```

**⚠️ Важно:**
- Домен должен быть **БЕЗ** `https://` и **БЕЗ** `/`
- Только домен: `app.outlivion.space`
- Telegram проверит, что домен доступен по HTTPS

---

### Шаг 2: Проверить переменные окружения

**Production (Vercel):**
```env
NEXT_PUBLIC_API_URL=https://api.outlivion.space
NEXT_PUBLIC_TELEGRAM_BOT_NAME=outlivionbot
```

**Development (Local):**
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_TELEGRAM_BOT_NAME=outlivionbot
```

---

## 🔄 Как работает авторизация

### 1. Пользователь нажимает кнопку Telegram Login Widget
```
┌──────────────────────┐
│  Login with Telegram │ ← Официальный виджет от Telegram
└──────────────────────┘
```

### 2. Telegram открывает OAuth окно
- Пользователь подтверждает авторизацию
- Telegram генерирует данные + hash подпись

### 3. Callback вызывается с данными пользователя
```typescript
{
  id: "123456789",
  first_name: "Иван",
  last_name: "Иванов",
  username: "ivan",
  photo_url: "https://...",
  auth_date: "1701234567",
  hash: "abc123..." ← Подпись от Telegram
}
```

### 4. Frontend отправляет данные на API
```typescript
POST https://api.outlivion.space/auth/telegram
Body: { id, first_name, ..., hash }
```

### 5. Backend проверяет подпись
```typescript
// Backend (outlivion-api)
// src/routes/auth.ts

1. Получает данные от frontend
2. Проверяет hash подпись с помощью TELEGRAM_BOT_TOKEN
3. Если валидно → создает/обновляет пользователя
4. Возвращает JWT tokens
```

### 6. Frontend сохраняет токены и редиректит
```typescript
// Tokens сохраняются в:
- localStorage (для Telegram Mini App)
- cookies (для Web Portal)

// Редирект на:
/web/dashboard
```

---

## 🧪 Тестирование

### Development (Localhost)

**Проблема:** Telegram Login Widget не работает на localhost

**Решения:**

#### Вариант A: Использовать туннель
```bash
# ngrok
ngrok http 3002

# localtunnel
npx localtunnel --port 3002

# Затем в BotFather:
/setdomain
<tunnel-url без https://>
```

#### Вариант B: Использовать production URL для теста
1. Задеплойте на Vercel (https://app.outlivion.space)
2. Настройте домен в BotFather
3. Тестируйте на production

---

### Production

1. **Откройте страницу:**
   ```
   https://app.outlivion.space/web/login
   ```

2. **Нажмите кнопку "Login with Telegram"**
   - Должно открыться окно Telegram OAuth
   - Подтвердите авторизацию

3. **Проверьте редирект:**
   - После успешного входа → `/web/dashboard`
   - Токены должны быть сохранены

4. **Проверьте Network tab:**
   ```
   POST /auth/telegram
   Status: 200
   Response: { accessToken, refreshToken, user }
   ```

---

## 🐛 Troubleshooting

### Ошибка: "Bot domain invalid"
**Причина:** Домен не настроен в BotFather

**Решение:**
```bash
# В @BotFather
/setdomain
app.outlivion.space
```

---

### Ошибка: "Telegram auth failed"
**Причина:** Backend не может проверить подпись

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` на backend
2. Проверьте что токен правильный
3. Проверьте логи backend

---

### Кнопка не загружается
**Причина:** Script Telegram не загрузился

**Решение:**
1. Проверьте консоль браузера
2. Проверьте что домен доступен по HTTPS
3. Попробуйте обновить страницу

---

### После входа не редиректит
**Причина:** Токены не сохраняются или ошибка API

**Решение:**
1. Проверьте Network tab → POST /auth/telegram
2. Проверьте что токены возвращаются
3. Проверьте localStorage или cookies

---

## 📋 Проверка после деплоя

- [ ] Домен настроен в BotFather (`/setdomain`)
- [ ] Переменные окружения установлены в Vercel
- [ ] Backend доступен (`https://api.outlivion.space`)
- [ ] Login page открывается (`https://app.outlivion.space/web/login`)
- [ ] Telegram Login Widget отображается
- [ ] Авторизация работает
- [ ] Редирект на dashboard после входа
- [ ] Токены сохраняются

---

## 🔐 Безопасность

### ✅ Что реализовано:

1. **Hash validation на backend**
   - Telegram подписывает данные с помощью bot token
   - Backend проверяет подпись перед созданием токенов

2. **JWT tokens**
   - Access token (1 час)
   - Refresh token (7 дней)

3. **HTTPS обязателен**
   - Telegram Login Widget работает только через HTTPS

4. **auth_date проверка**
   - Backend может проверить что данные не старше N минут

---

## 📚 Документация

### Официальная документация Telegram:
- [Telegram Login Widget](https://core.telegram.org/widgets/login)
- [Authentication](https://core.telegram.org/widgets/login#receiving-authorization-data)

### Наша документация:
- `src/lib/auth.ts` - Unified auth module
- `src/lib/api.ts` - API client
- `src/components/TelegramLoginButton.tsx` - Widget component

---

**Версия:** 1.0.0  
**Последнее обновление:** Декабрь 2025  
**Статус:** ✅ Production Ready

