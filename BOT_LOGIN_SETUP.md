# 🤖 Настройка Bot Login Flow

Руководство по настройке и тестированию авторизации через Telegram бота.

---

## 🎯 Как это должно работать

### Ожидаемый Flow:

```
1. Пользователь на сайте → Нажимает "Войти через Telegram"
2. Frontend → Создает token → Открывает t.me/outlivionbot?start=login_TOKEN
3. Telegram → Открывается бот → Показывается кнопка START
4. Пользователь → Нажимает START
5. Telegram → Отправляет боту: /start login_TOKEN
6. Бот → Обрабатывает команду → Вызывает webhook
7. Webhook → Подтверждает логин → Возвращает результат
8. Frontend → Получает токены → Редиректит на dashboard
```

---

## ✅ Текущая настройка

### Webhook установлен:
```json
{
  "url": "https://app.outlivion.space/api/bot",
  "pending_update_count": 0,
  "allowed_updates": ["message", "callback_query"]
}
```

### Код обработки уже есть:

```typescript
// src/app/api/bot/route.ts:170-180
if (text.startsWith('/start')) {
  const loginToken = parseLoginCommand(text)
  
  if (loginToken) {
    // /start login_TOKEN → handleDeepLinkLogin
    await handleDeepLinkLogin(chatId, message.from, loginToken)
  } else {
    // /start → обычное приветствие
    await handleStartCommand(chatId, message.from)
  }
}
```

---

## 🔍 Проблема: Кнопка Start не появляется

### Возможные причины:

#### 1. Бот уже был запущен ранее

**Проблема:** 
Telegram НЕ показывает кнопку START если:
- Пользователь уже нажимал /start этого бота раньше
- Есть история сообщений с ботом

**Решение:**
Бот должен отправить сообщение автоматически при переходе по deep-link!

#### 2. Webhook не получает /start команду

**Проблема:**
Deep-link параметр теряется если бот уже активен.

**Решение:**
Использовать альтернативный механизм.

---

## 🛠️ Решение: Улучшить обработку deep-link

### Вариант 1: Отправлять сообщение автоматически (Рекомендуется)

Когда пользователь переходит по `t.me/outlivionbot?start=login_TOKEN`, бот должен:

1. **Получить update с `start_param`**
2. **Сразу обработать без ожидания кнопки Start**
3. **Отправить подтверждающее сообщение**

### Вариант 2: Использовать Inline кнопку

Вместо ожидания /start, показать inline кнопку "Подтвердить вход".

---

## 📝 Код для исправления

Нужно обновить обработчик `/start` чтобы он работал для новых и существующих пользователей:

```typescript
// В handleMessage (src/app/api/bot/route.ts)
if (text.startsWith('/start')) {
  const loginToken = parseLoginCommand(text)
  
  if (loginToken) {
    // СРАЗУ обрабатываем login без ожидания
    await handleDeepLinkLogin(chatId, message.from, loginToken)
    return // Важно! Выходим чтобы не показывать обычное приветствие
  }
  
  // Обычный /start
  await handleStartCommand(chatId, message.from)
}
```

**Это уже реализовано в коде!** ✅

---

## 🧪 Как протестировать

### Тест 1: Создать login token и проверить

```bash
cd outlivion-miniapp
./test-bot-login.sh
```

Скопируйте Bot URL и откройте в Telegram.

### Тест 2: Проверить что webhook получает команду

1. Откройте bot URL: `https://t.me/outlivionbot?start=login_TOKEN`
2. Нажмите Start (если показывается)
3. Проверьте логи webhook:

```bash
# Если запущено на Vercel
vercel logs --follow

# Если локально
npm run dev
# Смотрите консоль
```

### Тест 3: Проверить обработку в боте

После нажатия Start вы должны увидеть:

**В боте:**
```
✅ Авторизация подтверждена!

Привет, Имя! Вы успешно вошли в свой аккаунт.

🌐 Вернитесь на сайт, чтобы продолжить.
```

**В браузере (где ждем):**
```
✅ Успешный вход!
→ Редирект на dashboard
```

---

## 🐛 Debugging

### Проверка 1: Webhook получает updates?

```bash
curl -s "https://api.telegram.org/bot<TOKEN>/getUpdates?limit=1&offset=-1" | python3 -m json.tool
```

Должны увидеть последний update с вашей командой `/start login_...`

### Проверка 2: Логи webhook

```bash
# Откройте в браузере
https://app.outlivion.space/api/bot

# Должен показать статус webhook
```

### Проверка 3: Database check

```bash
# В outlivion-api
node -e "
require('dotenv').config();
const { Pool } = require('pg');
const pool = new Pool({ 
  connectionString: process.env.DATABASE_URL,
  ssl: { rejectUnauthorized: false }
});
pool.query('SELECT token, status, telegram_id, created_at FROM login_sessions ORDER BY created_at DESC LIMIT 5', (err, res) => {
  if (err) console.error(err);
  else console.table(res.rows);
  pool.end();
});
"
```

Должны увидеть session со статусом `approved` после нажатия Start.

---

## 🔧 Возможная проблема: Бот не отвечает

### Если бот молчит при /start login_TOKEN:

**Причина:**
1. Webhook не получает update
2. Код падает с ошибкой
3. API недоступен

**Диагностика:**

```bash
# 1. Проверить webhook работает
curl https://app.outlivion.space/api/bot

# 2. Проверить что бот получает updates
curl "https://api.telegram.org/bot<TOKEN>/getUpdates"

# 3. Проверить логи Vercel
vercel logs --since 5m
```

**Решение:**

1. **Убедитесь webhook endpoint работает:**
   ```bash
   curl https://app.outlivion.space/api/bot
   # Должен вернуть статус страницу
   ```

2. **Проверьте environment variables на Vercel:**
   - `TELEGRAM_BOT_TOKEN` установлен
   - `NEXT_PUBLIC_API_URL` правильный

3. **Перезапустите webhook:**
   ```bash
   npm run setup:webhook
   ```

---

## 💡 Альтернативное решение: Inline кнопка

Если Start не показывается, можно использовать inline кнопку:

```typescript
// Вместо ожидания Start, отправить сообщение с кнопкой
await sendMessage(chatId, '🔐 Подтвердите вход в Outlivion Web Portal', {
  reply_markup: {
    inline_keyboard: [
      [
        {
          text: '✅ Подтвердить вход',
          callback_data: `confirm_login_${token}`
        }
      ],
      [
        {
          text: '❌ Отменить',
          callback_data: 'cancel_login'
        }
      ]
    ]
  }
})
```

Но это требует изменений в коде бота.

---

## 📞 Нужна помощь?

**Если Start всё еще не появляется:**

1. Попробуйте в **Incognito/Private** режиме Telegram
2. Или используйте другой Telegram аккаунт для теста
3. Или добавьте inline кнопку (см. выше)

**Для проверки логов бота:**
```bash
# Vercel deployment
vercel logs --follow

# Ищите:
[BOT] Handling deep-link login: { chatId: 123, token: 'f524...' }
```

---

**Created:** December 2025  
**Version:** 2.0.0

